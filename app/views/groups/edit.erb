<table>
  <tr>
    <th></th>
    <th>Deputy</th>
    <th>Value</th>
    <th>time</th>
  </tr>
  <% @group.reports(:join => :deputy, :include => :deputy, :sort => 'deputies.name').each do |report| %>
    <%= render :partial => 'reports/report_row', :locals => {:report => report, :hide_group => true} %>
  <% end %>
</table>