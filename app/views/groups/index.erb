<%= paginate collection %>
<table class="sharp">
  <% collection.each do |group| %>
    <tr>
      <td><%= link_to_object group %></td>
      <td><%= button_to_delete group, :text => 'x' %></td>
    </tr>
  <% end %>
</table>
<%= paginate collection %>
