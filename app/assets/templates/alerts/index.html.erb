<%= partial 'shared/header_pagination' %>

<table class='sharp'>
  <tr>
    <th>Deputy</th>
    <th>Report</th>
    <th>Act</th>
  </tr>
  <tr ng:repeat="alert in alerts().get()">
    <td>
      <span class="label label-info">
        <a href="">{{alert.deputy.name}}</a>

      </span>
    </td>
    <td class='max_width_200'>
      <span class='label' ng:class="color_error(alert.error_level)">{{alert.report.group_full_name}}</span> <br>
      {{alert.message}}
    </td>
    <td>
      <a ng:href='{{routes.alertsEditUrl(alert)}}' class='btn btn-mini'>Edit</a> &nbsp;
      <a ng:href='{{routes.alertsDestroyUrl(alert)}}' class='btn btn-mini btn-danger'>X</a>
    </td>
  </tr>
</table>
<%= partial 'shared/footer_pagination' %>
